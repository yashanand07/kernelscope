from semantic_runtime.compiler.indices import CompilerIndices

# Let's inspect the underlying fields of the Phase 0 index model
print("--- CollectionIndex Attributes ---")
# Replace with how you grab indices in main_runner if needed,
# or just print the class dictionary definition:
from semantic_runtime.compiler.indices import CollectionIndex
print(dir(CollectionIndex))
